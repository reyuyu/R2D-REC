(() => {
  const previousRenderAdvantages = renderAdvantages;
  const previousRenderProbes = renderProbes;
  const routeOptions = $('advRouteSelect').innerHTML;
  const focusOptions = $('advFocusSelect').innerHTML;
  const style = document.createElement('style');
  style.textContent = `
    .v5-dashboard{color:var(--ink)}.v5-charts{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:14px}.v5-chart-panel{border:1px solid var(--line);border-top:3px solid #287b64;background:#fff;padding:12px;min-width:0}.v5-chart-panel h3{margin:0 0 3px;font-size:13px;color:var(--ink)}.v5-chart-panel p{margin:0 0 6px;color:var(--muted);font-size:10px}.v5-chart{width:100%;height:205px;display:block}
    .v5-kpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));border:1px solid var(--line);background:#fff;margin-bottom:14px}.v5-kpi{padding:10px 12px;border-right:1px solid var(--line);min-width:0}.v5-kpi:last-child{border:0}.v5-kpi b{display:block;font-size:17px;color:#17212b}.v5-kpi span{font-size:10px;color:#68747d}.v5-contract{padding:9px 11px;border-left:3px solid #287b64;background:#eef6f3;color:#34584d;font-size:11px;margin-bottom:12px}.v5-contract b{color:#163d32}
    .v5-group{border:1px solid var(--line);border-top:3px solid #263c48;background:#fff}.v5-group-head{display:flex;justify-content:space-between;gap:12px;padding:12px 14px;background:#f7f9f8;border-bottom:1px solid var(--line)}.v5-group-head .summary-chips{max-width:62%}.v5-cot-overview{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;padding:12px 12px 2px}.v5-cot-score{padding:10px 11px;border:1px solid #d5dde1;border-left:4px solid #87949c;background:#f5f7f7;color:#33424a}.v5-cot-score strong{display:block;margin-bottom:5px;color:#17212b;font-size:13px}.v5-cot-score b{font-size:17px}.v5-cot-score span{display:block;margin-top:3px;font-size:10px}.v5-cot-score.positive{border-color:#85bea5;border-left-color:#16835f;background:#e9f7f0;color:#0a6749}.v5-cot-score.negative{border-color:#dda4ab;border-left-color:#bd3f4c;background:#fff0f2;color:#9a2d3b}.v5-cot-score.zero{background:#f1f3f4;color:#637078}.v5-source,.v5-cot{border:1px solid var(--line);margin:10px 12px;background:#fff}.v5-source>summary,.v5-cot>summary{display:flex;justify-content:space-between;gap:10px;padding:10px 11px;background:#f0f4f2;color:#17212b;font-weight:700;cursor:pointer;list-style:none}.v5-source>summary::-webkit-details-marker,.v5-cot>summary::-webkit-details-marker{display:none}.v5-cot.positive{border-color:#82bca2}.v5-cot.positive>summary{background:#e7f6ee;color:#095f45}.v5-cot.negative{border-color:#dba0a8}.v5-cot.negative>summary{background:#ffedef;color:#922b38}.v5-cot.zero>summary{background:#f0f2f3;color:#56636b}.v5-cot-scoreline{font-size:13px}.v5-cot-scoreline b{font-size:15px}.v5-body{padding:10px}.v5-raw{margin:7px 0;max-height:310px;overflow:auto;padding:9px;border:1px solid #dce3df;background:#f8faf9;color:#263740;font:11px/1.55 ui-monospace,SFMono-Regular,Consolas,monospace;white-space:pre-wrap;overflow-wrap:anywhere}.v5-cot-stats{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:6px;margin-bottom:8px}.v5-mini{padding:7px 8px;border-left:3px solid #79968a;background:#f5f8f7}.v5-mini b{display:block;color:#17212b}.v5-mini span{font-size:10px;color:#66737b}
    .v5-table{width:100%;border-collapse:collapse;font-size:11px}.v5-table th,.v5-table td{padding:8px;border-bottom:1px solid #dfe5e8;text-align:right;vertical-align:top}.v5-table th:nth-child(2),.v5-table td:nth-child(2){text-align:left}.v5-table th{background:#f7f9f8;color:#59666f;white-space:nowrap}.v5-table tr.v5-adv-positive{background:#e9f7f0;box-shadow:inset 4px 0 #16835f}.v5-table tr.v5-adv-negative{background:#fff0f2;box-shadow:inset 4px 0 #bd3f4c}.v5-table tr.v5-adv-zero{background:#f3f5f5;box-shadow:inset 4px 0 #8b969d}.v5-table tr.copy td:nth-child(6){background:#ffe2e6}.v5-output{font:11px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace;color:#273740;overflow-wrap:anywhere}.v5-copy,.v5-noncopy,.v5-rule,.v5-adv-pill{display:inline-block;padding:3px 7px;border:1px solid;border-radius:3px;font-size:10px;font-weight:800;white-space:nowrap}.v5-copy{border-color:#c36a78;background:#fff0f2;color:#8f2437}.v5-noncopy{border-color:#79af98;background:#eef8f3;color:#176349}.v5-rule{border-color:#ca9444;background:#fff4df;color:#7a500f}.v5-adv-pill.positive{border-color:#65a98a;background:#dff3e8;color:#086344}.v5-adv-pill.negative{border-color:#d58d97;background:#ffe2e6;color:#992b39}.v5-adv-pill.zero{border-color:#aeb8bd;background:#e8ecee;color:#56636b}.v5-score-flow{white-space:nowrap;font-variant-numeric:tabular-nums}.v5-score-flow b{font-size:13px;color:#17212b}.v5-probe-suite{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:12px}.v5-beams{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:4px;margin-top:7px}.v5-beam{padding:5px 6px;border-left:3px solid #9eaaa5;background:#f5f7f6;color:#3c4b53;font:10px/1.35 ui-monospace,SFMono-Regular,Consolas,monospace;overflow-wrap:anywhere}.v5-beam.copy{border-color:#bd3f4c;background:#fff0f2}.v5-beam.exact{box-shadow:inset 3px 0 #16835f}.v5-beam.ab{box-shadow:inset 3px 0 #3479bf}.v5-beam.a{box-shadow:inset 3px 0 #bd8128}.v5-probe-cot{margin:7px 0;padding:8px;border:1px solid #dce3df;background:#f8faf9;color:#273740;font:11px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace;white-space:pre-wrap;max-height:250px;overflow:auto}.v5-probe-table td{text-align:left}.v5-muted{color:#68747d!important}
    .v5-domain{display:inline-block;padding:3px 8px;border:1px solid;border-radius:3px;font-size:10px;font-weight:900;text-transform:uppercase;vertical-align:middle}.v5-domain.video{color:#8f2437;background:#fff0f2;border-color:#cf8290}.v5-domain.ad{color:#72510b;background:#fff4d8;border-color:#d7a843}.v5-domain.prod{color:#174f86;background:#eaf3fc;border-color:#6fa2d2}.v5-domain.living{color:#176349;background:#e7f6ee;border-color:#70ad90}.v5-domain-legend{display:flex;gap:6px;flex-wrap:wrap;margin:6px 0 10px}.v5-copy-sid{display:block;margin-top:5px;padding:5px 6px;border-left:3px solid #bd3f4c;background:#fff0f2;color:#7f2032;font:10px/1.4 ui-monospace,SFMono-Regular,Consolas,monospace}.v5-table tr.copy td{background:#fff8f9}.v5-table tr.copy td:nth-child(6){background:#ffdce2}.v5-beam.copy{background:#ffe9ed;color:#781c2d}
    .v5-probe-nav{display:flex;gap:0;margin:0 0 12px;border-bottom:1px solid var(--line)}.v5-probe-nav button{border:1px solid var(--line);border-bottom:0;background:#f3f6f5;color:#56636b;padding:9px 14px;font-weight:800;cursor:pointer}.v5-probe-nav button.active{background:#173c33;color:#fff;border-color:#173c33}.v5-history-callout{border-left-color:#bd3f4c;background:#fff1f3;color:#762336}
    @media(max-width:1100px){.v5-kpis,.v5-cot-stats{grid-template-columns:repeat(3,1fr)}.v5-cot-overview{grid-template-columns:repeat(2,1fr)}.v5-beams{grid-template-columns:repeat(2,1fr)}}@media(max-width:760px){.v5-charts,.v5-probe-suite{grid-template-columns:1fr}.v5-kpis,.v5-cot-stats,.v5-cot-overview{grid-template-columns:repeat(2,1fr)}.v5-group-head{display:block}.v5-group-head .summary-chips{max-width:none;margin-top:8px}.v5-beams{grid-template-columns:1fr}}
  `;
  document.head.appendChild(style);

  const detailState = new Map();
  let probePage='standard';
  function detailAttr(key,defaultOpen=false){const open=detailState.has(key)?detailState.get(key):defaultOpen;return `data-v5-detail="${escapeHtml(key)}"${open?' open':''}`}
  document.addEventListener('toggle',event=>{const details=event.target.closest?.('.v5-dashboard details[data-v5-detail]');if(details)detailState.set(details.dataset.v5Detail,details.open)},true);
  document.addEventListener('click',event=>{const button=event.target.closest?.('[data-v5-probe-page]');if(!button)return;probePage=button.dataset.v5ProbePage;renderProbes()},true);

  const domainOrder=['video','ad','prod','living'],domainColors={video:'#bd3f4c',ad:'#c28a1e',prod:'#3479bf',living:'#16835f'};
  function isMixedV5(){return Boolean(state.capabilities?.official_anticopy_mixed_v5||state.manifest?.experiment==='GR_REC_OfficialAntiCopy_Mixed_v5')}
  function isV5(){return Boolean(isMixedV5()||state.capabilities?.video_official_anticopy_v5||state.manifest?.experiment==='GR_REC_VideoOfficialAntiCopy_v5')}
  function domainBadge(domain){const value=domainOrder.includes(domain)?domain:'video';return `<span class="v5-domain ${value}">${escapeHtml(value)}</span>`}
  function domainLegend(){return `<div class="v5-domain-legend">${domainOrder.map(domain=>domainBadge(domain)).join('')}</div>`}
  function signedV5(value,digits=3){const number=Number(value);return Number.isFinite(number)?`${number>0?'+':''}${number.toFixed(digits)}`:'-'}
  function sidV5(value){return Array.isArray(value)?`${value[0]}:${value.slice(1).join('/')}`:'INVALID'}
  function groupCopy(group){const rows=group.cots.flatMap(cot=>cot.candidates);return {count:rows.filter(row=>row.is_history_copy).length,total:rows.length,removed:rows.filter(row=>row.anti_copy_removed).length,copyPositive:rows.filter(row=>row.is_history_copy&&row.sid_advantage>0).length,noncopyPositive:rows.filter(row=>!row.is_history_copy&&row.final_sid_reward>0).length}}
  function configureControls(){
    if($('advRouteSelect').dataset.v5)return;
    $('advRouteSelect').dataset.v5='1';
    $('advRouteSelect').innerHTML='<option value="">4 个 Official G8</option>';
    $('advFocusSelect').innerHTML='<option value="">全部候选</option><option value="copy">抄历史答案</option><option value="noncopy">非抄写答案</option><option value="removed">正奖励已清零</option><option value="positive_adv">正 Advantage</option><option value="positive_reward">正最终 Reward</option><option value="exact">Exact</option>';
  }
  function restoreControls(){if(!$('advRouteSelect').dataset.v5)return;delete $('advRouteSelect').dataset.v5;$('advRouteSelect').innerHTML=routeOptions;$('advFocusSelect').innerHTML=focusOptions}
  function filtered(rows,focus){return rows.filter(row=>!focus||focus==='copy'&&row.is_history_copy||focus==='noncopy'&&!row.is_history_copy||focus==='removed'&&row.anti_copy_removed||focus==='positive_adv'&&row.sid_advantage>0||focus==='positive_reward'&&row.final_sid_reward>0||focus==='exact'&&row.raw_q_reward===8)}
  function candidateRow(row,key){
    const rule=row.anti_copy_removed?'<span class="v5-rule">抄写奖励清零</span>':row.copied_exact_preserved?'<span class="v5-rule">Video copied Exact 保留 8</span>':row.copy_discounted?`<span class="v5-rule">Copy soft discount ${fmt(row.reward_delta)}</span>`:'<span class="v5-muted">未触发改写</span>';
    const tone=creditTone(row.sid_advantage);
    const copyMark=row.is_history_copy?`<span class="v5-copy">历史抄写 SID</span><span class="v5-copy-sid">${escapeHtml(sidV5(row.copied_history_sid||row.sid))}</span>`:'<span class="v5-noncopy">非抄写</span>';
    return `<tr class="${row.is_history_copy?'copy ':'noncopy '}v5-adv-${tone}"><td>#${row.candidate_id+1}<br>${domainBadge(row.target_domain)}</td><td><div class="v5-output">${escapeHtml(row.completion_text||sidV5(row.sid))}</div><details ${detailAttr(`${key}:candidate:${row.candidate_id}`)}><summary>采样 / token / SID</summary><div class="v5-contract">${escapeHtml(row.sampling_contract)} · action mask <b>${row.masked_action_token_count} token</b></div><div class="v5-output">candidate SID ${escapeHtml(sidV5(row.sid))}<br>${row.is_history_copy?`matched history SID ${escapeHtml(sidV5(row.copied_history_sid||row.sid))}<br>`:''}token ids ${(row.token_ids||[]).join(', ')}</div></details></td><td><b>${escapeHtml(row.reward_level)}</b></td><td class="v5-score-flow">${fmt(row.raw_q_reward)} → <b>${fmt(row.final_sid_reward)}</b></td><td><span class="v5-adv-pill ${tone}">${signedV5(row.sid_advantage,4)}</span></td><td>${copyMark}</td><td><b>${fmt(row.cot_contribution)}</b></td><td>${rule}</td></tr>`
  }
  function renderCot(cot,focus,groupKeyValue){
    const s=cot.summary||{},rows=filtered(cot.candidates||[],focus);
    const tone=creditTone(cot.cot_advantage),key=`adv:${groupKeyValue}:cot:${cot.cot_id}`;
    return `<details class="v5-cot ${tone}" ${detailAttr(key,cot.cot_id===0)}><summary><span class="v5-cot-scoreline">${domainBadge(cot.target_domain)} CoT ${cot.cot_id+1} · reward <b>${fmt(cot.cot_reward)}</b> · G4 advantage <span class="v5-adv-pill ${tone}">${signedV5(cot.cot_advantage,4)}</span></span><span class="summary-chip">抄写 ${s.copy_count??0}/8 · 展开</span></summary><div class="v5-body"><div class="v5-cot-stats"><div class="v5-mini"><b>${cot.cot_length??'-'}</b><span>CoT token</span></div><div class="v5-mini"><b>${fmt(s.raw_reward_mean)} / ${fmt(s.final_reward_mean)}</b><span>raw μ / shaped μ</span></div><div class="v5-mini"><b>${fmt(s.advantage_std)}</b><span>独立 G8 advantage σ</span></div><div class="v5-mini"><b>${s.copy_discounted_count??s.anti_copy_removed_count??0}</b><span>copy reward 改写</span></div><div class="v5-mini"><b>${cot.copy_positive_advantage_count??0}</b><span>copy 正 advantage</span></div><div class="v5-mini"><b>${cot.noncopy_positive_reward_count??0}</b><span>non-copy 正 reward</span></div></div><details ${detailAttr(`${key}:raw`)}><summary>模型 CoT 输出原文</summary><pre class="v5-raw">${escapeHtml(cot.cot_text||'')}</pre><div class="v5-contract">随后固定插入 <b>${escapeHtml(cot.official_domain_prefix||'<|video_begin|>')}</b>，再随机采样 ABC 三 token。</div></details><div class="table-scroll"><table class="v5-table"><thead><tr><th># / 域</th><th>Official G8 输出原文</th><th>层级</th><th>Raw → Shaped</th><th>G8 Advantage</th><th>历史抄写 SID</th><th>CoT credit</th><th>规则</th></tr></thead><tbody>${rows.map(row=>candidateRow(row,key)).join('')||'<tr><td colspan="8">当前筛选无候选</td></tr>'}</tbody></table></div></div></details>`
  }
  function cotOverview(cots){return `<div class="v5-cot-overview">${cots.map(cot=>{const tone=creditTone(cot.cot_advantage);return `<div class="v5-cot-score ${tone}"><strong>${domainBadge(cot.target_domain)} CoT ${cot.cot_id+1}</strong><b>Reward ${fmt(cot.cot_reward)}</b><span>G4 Advantage ${signedV5(cot.cot_advantage,4)}</span></div>`}).join('')}</div>`}
  function rolloutSeries(groups,key){return groups.map(group=>[group.step,key(group)])}
  function optimizationSeries(rows,key,iteration){return rows.filter(row=>Number(row.policy_iteration)===iteration).map(row=>[row.step,Number(row[key])])}
  function groupsByDomain(groups,metric){return domainOrder.map(domain=>({data:rolloutSeries(groups.filter(group=>group.target_domain===domain),metric),color:domainColors[domain]}))}
  function mixedRewardContract(group){if(!isMixedV5())return 'Video：copy non-Exact→0；copied Exact SID reward 保留 8；所有 copy 的 CoT credit 为 0。';const stage=group.reward_stage||'-';return `Video：copy non-Exact→0、copied Exact→8、copy CoT credit→0。Ad/Prod/Living：non-copy 保持 raw；copy 在 ${stage} 阶段使用 domain-aware soft discount，CoT credit 等于 shaped SID reward。`}
  function renderV5Advantages(){
    configureControls();
    const groups=(state.advantages?.groups||[]).filter(group=>group.valid&&['video_official_anticopy_v5','official_anticopy_mixed_v5'].includes(group.kind)),select=$('advGroupSelect'),current=select.value;
    select.innerHTML=groups.length?[...groups].reverse().map(group=>`<option value="${escapeHtml(groupKey(group))}">[${String(group.target_domain).toUpperCase()}] Step ${group.step} · ${escapeHtml(String(group.group_id).slice(0,32))}</option>`).join(''):'<option value="">等待 V5 rollout</option>';
    if(groups.some(group=>groupKey(group)===current))select.value=current;
    const selected=groups.find(group=>groupKey(group)===select.value)||groups.at(-1),optimization=state.advantages?.optimization||[];
    if(!selected){$('advantageContent').innerHTML='<div class="adv-empty">等待 V5 rollout 明细。</div>';return}
    select.value=groupKey(selected);const selectedKey=groupKey(selected),focus=$('advFocusSelect').value,copy=groupCopy(selected),golds=(selected.gold_sids||[]).map(value=>`<span class="gold-chip">${escapeHtml(typeof value==='string'?value:sidV5(value))}</span>`).join('');
    $('advantageContent').innerHTML=`<div class="v5-dashboard">${domainLegend()}<div class="v5-kpis"><div class="v5-kpi"><b>${copy.count}/${copy.total}</b><span>当前域 Official candidates 抄写</span></div><div class="v5-kpi"><b>${copy.removed}</b><span>copy reward 清零</span></div><div class="v5-kpi"><b>${copy.copyPositive}</b><span>copy 正 advantage</span></div><div class="v5-kpi"><b>${copy.noncopyPositive}</b><span>non-copy 正 reward</span></div><div class="v5-kpi"><b>${fmt(selected.cots.reduce((a,c)=>a+Number(c.cot_length||0),0)/4,1)}</b><span>mean CoT length</span></div><div class="v5-kpi"><b>4 × G8</b><span>独立 normalization；32 仅总候选数</span></div></div><div class="v5-charts"><section class="v5-chart-panel"><h3>四域训练 History Copy Rate</h3><p>每个 fresh rollout 的 32 个候选；红 Video / 黄 Ad / 蓝 Prod / 绿 Living</p><canvas id="v5CopyChart" class="v5-chart"></canvas></section><section class="v5-chart-panel"><h3>四域 Shaped Reward Mean</h3><p>训练 shaped SID reward；按业务域分开，不与 Probe production reward 混用</p><canvas id="v5DomainRewardChart" class="v5-chart"></canvas></section><section class="v5-chart-panel"><h3>Policy iteration 1 / 2 · SID ratio</h3><p>同一 rollout 复用的两次更新</p><canvas id="v5RatioChart" class="v5-chart"></canvas></section><section class="v5-chart-panel"><h3>SID Approx KL</h3><p>iteration 2 是否过激的直接诊断</p><canvas id="v5KlChart" class="v5-chart"></canvas></section></div><div class="v5-contract"><b>得分合同：</b>Raw q_reward 为 invalid −1 / wrong-domain −0.25 / domain 0 / A 0.5 / AB 2 / Exact 8。${escapeHtml(mixedRewardContract(selected))}</div><section class="v5-group"><header class="v5-group-head"><div><div class="adv-title">${domainBadge(selected.target_domain)} Step ${selected.step} · ${escapeHtml(selected.group_id)}</div><div class="adv-sub">fresh rollout ${selected.fresh_rollout_index??'-'} · reward stage ${escapeHtml(selected.reward_stage||'-')} · ${escapeHtml(selected.normalization_topology||'G4 + 4 independent Official G8')} · fingerprint ${escapeHtml(selected.rollout_fingerprint||'-')}</div></div><div class="summary-chips"><span class="summary-chip alert">抄历史 ${copy.count}/32</span><span class="summary-chip">清零 ${copy.removed}</span><span class="summary-chip good">non-copy 正信号 ${copy.noncopyPositive}</span></div></header>${cotOverview(selected.cots)}<details class="v5-source" ${detailAttr(`adv:${selectedKey}:source`,true)}><summary><span>模型输入原文</span><span>Gold / history 可展开核对</span></summary><div class="v5-body"><pre class="v5-raw">${escapeHtml(selected.input_prompt||'源数据校验失败，暂不可用')}</pre><div class="gold-strip"><span class="label">Gold SID（reward reference）</span>${golds}</div><details ${detailAttr(`adv:${selectedKey}:history`)}><summary>历史完整 ${escapeHtml(String(selected.target_domain).toUpperCase())} SID（copy 判定集合）</summary><div class="v5-raw">${escapeHtml((selected.history_sids||[]).map(sidV5).join('\n'))}</div></details></div></details>${selected.cots.map(cot=>renderCot(cot,focus,selectedKey)).join('')}</section></div>`;
    const copyRate=group=>{const c=groupCopy(group);return c.total?c.count/c.total:0},noncopy=group=>{const c=groupCopy(group);return c.total?c.noncopyPositive/c.total:0};
    draw('v5CopyChart',groupsByDomain(groups,copyRate));
    draw('v5DomainRewardChart',groupsByDomain(groups,group=>{const rows=group.cots.flatMap(cot=>cot.candidates);return rows.length?rows.reduce((sum,row)=>sum+Number(row.final_sid_reward),0)/rows.length:0}));
    draw('v5RatioChart',[{data:optimizationSeries(optimization,'sid_ratio_mean',1),color:colors[0]},{data:optimizationSeries(optimization,'sid_ratio_mean',2),color:colors[3]}]);
    draw('v5KlChart',[{data:optimizationSeries(optimization,'sid_approx_kl',1),color:colors[0]},{data:optimizationSeries(optimization,'sid_approx_kl',2),color:colors[3]}]);
  }
  renderAdvantages=function(){if(isV5())renderV5Advantages();else{restoreControls();previousRenderAdvantages()}};

  function suite(row){if(row.probe_suite==='history_copy_exact')return 'History-Copy Exact Probe4';if(row.probe_suite==='heldout_video_official')return 'Video Official Probe12';return isMixedV5()?'Official Probe16':'Original Official Probe4'}
  function averageAt(rows,key){const by=new Map();for(const row of rows){const value=key.split('.').reduce((x,k)=>x?.[k],row);if(value==null)continue;if(!by.has(row.step))by.set(row.step,[]);by.get(row.step).push(Number(value))}return [...by].sort((a,b)=>a[0]-b[0]).map(([step,v])=>[step,v.reduce((a,b)=>a+b,0)/v.length])}
  function probeSlot(rows,row){const peers=[...new Map(rows.filter(x=>x.target_domain===row.target_domain).map(x=>[x.group_id,x])).keys()];const index=peers.indexOf(row.group_id);return index<0?'-':`${index+1}/${peers.length}`}
  function probeCoverage(rows,step,perDomain=4){const atStep=[...new Map(rows.filter(row=>row.step===step).map(row=>[row.group_id,row])).values()],counts=Object.fromEntries(domainOrder.map(domain=>[domain,atStep.filter(row=>row.target_domain===domain).length]));return {rows:atStep,total:atStep.length,counts,expected:perDomain*4,perDomain,complete:atStep.length===perDomain*4&&domainOrder.every(domain=>counts[domain]===perDomain)}}
  function beamCell(detail,index,domain){const level=String(detail.reward_level||'domain').toLowerCase(),copy=detail.is_history_copy,sid=sidV5(detail.candidate_sid);return `<div class="v5-beam ${copy?'copy ':''}${level}">${domainBadge(domain)} <b>#${index+1}</b> ${escapeHtml(sid)}<br>${escapeHtml(detail.reward_level||'-')} · production q ${fmt(detail.raw_q_reward)}<br>${copy?`<span class="v5-copy">历史抄写 SID</span><span class="v5-copy-sid">${escapeHtml(sid)}</span>`:'<span class="v5-noncopy">非抄写</span>'}</div>`}
  function probeCandidates(row,probeKey){const data=row.think||{},domain=row.target_domain;return (data.candidates||[]).map((candidate,index)=>{const beams=candidate.beam_candidate_details||[],tone=Number(candidate.reward)>0?'positive':'zero',key=`${probeKey}:cot:${index}`;return `<details class="v5-cot ${tone}" ${detailAttr(key,index===0)}><summary><span class="v5-cot-scoreline">${domainBadge(domain)} CoT ${index+1} · reward <b>${fmt(candidate.reward)}</b> · ${candidate.exact??0}/${candidate.ab??0}/${candidate.a??0} (Exact/AB/A)</span><span>${candidate.history_copy_count??beams.filter(x=>x.is_history_copy).length}/32 copy</span></summary><div class="v5-body"><div class="v5-contract">Official Beam32 ABC3 · candidate denominator <b>${data.beam_candidate_count??128}</b> / CoT denominator <b>${data.candidate_count??4}</b> · production reward，不应用训练 copy discount · ${escapeHtml(data.reward_semantics||'')}</div><details ${detailAttr(`${key}:raw`,true)}><summary>CoT 模型输出原文</summary><pre class="v5-probe-cot">${escapeHtml(candidate.completion||'')}</pre></details><div class="v5-beams">${beams.map((beam,i)=>beamCell(beam,i,domain)).join('')}</div></div></details>`}).join('')}
  function renderV5Probes(){
    const groupSelect=$('probeGroupSelect'),stepSelect=$('probeStepSelect'),currentGroup=groupSelect.value,allRows=state.probes.filter(row=>row.think&&typeof row.think==='object'),history=allRows.filter(row=>row.probe_suite==='history_copy_exact'),standard=allRows.filter(row=>row.probe_suite!=='history_copy_exact'),rows=probePage==='history'?history:standard,groups=[...new Map(rows.map(row=>[row.group_id,row])).entries()];
    groupSelect.innerHTML=groups.map(([gid,row])=>`<option value="${escapeHtml(gid)}">[${String(row.target_domain).toUpperCase()} ${probeSlot(rows,row)}] ${suite(row)} · ${escapeHtml(gid.slice(0,10))}</option>`).join('');if(groups.some(([gid])=>gid===currentGroup))groupSelect.value=currentGroup;
    const available=rows.filter(row=>row.group_id===groupSelect.value).sort((a,b)=>a.step-b.step),current=Number(stepSelect.value);stepSelect.innerHTML=[...available].reverse().map(row=>`<option value="${row.step}">Step ${row.step} · ${row.reason}</option>`).join('');if(available.some(row=>row.step===current))stepSelect.value=String(current);
    const selected=available.find(row=>row.step===Number(stepSelect.value))||available.at(-1),original=standard.filter(row=>row.probe_suite!=='heldout_video_official'),heldout=standard.filter(row=>row.probe_suite==='heldout_video_official');
    $('probeRewardChart').closest('.panel').hidden=false;
    if(isMixedV5()&&probePage==='history'){setChartPanel('probeRewardChart','History-Copy Exact Probe4 · Production Reward',domainOrder.map(x=>x.toUpperCase()));setChartPanel('probeClosureChart','History-Copy Exact Probe4 · Beam Copy Rate',domainOrder.map(x=>x.toUpperCase()));draw('probeRewardChart',domainOrder.map(domain=>({data:averageAt(history.filter(row=>row.target_domain===domain),'think.reward_mean'),color:domainColors[domain]})));draw('probeClosureChart',domainOrder.map(domain=>({data:averageAt(history.filter(row=>row.target_domain===domain),'think.history_copy_rate'),color:domainColors[domain]})))}else if(isMixedV5()){setChartPanel('probeRewardChart','Official Probe16 Production Reward · 四域均值',domainOrder.map(x=>`${x.toUpperCase()} · 4 groups`));setChartPanel('probeClosureChart','Official Probe16 History Copy Rate · 四域均值',domainOrder.map(x=>`${x.toUpperCase()} · 4 groups`));draw('probeRewardChart',domainOrder.map(domain=>({data:averageAt(original.filter(row=>row.target_domain===domain),'think.reward_mean'),color:domainColors[domain]})));draw('probeClosureChart',domainOrder.map(domain=>({data:averageAt(original.filter(row=>row.target_domain===domain),'think.history_copy_rate'),color:domainColors[domain]})))}else{setChartPanel('probeRewardChart','Official Probe Reward 趋势',['Original Probe4','Video Probe12']);setChartPanel('probeClosureChart','Official Probe History Copy Rate',['Original Probe4','Video Probe12']);draw('probeRewardChart',[{data:averageAt(original,'think.reward_mean'),color:colors[0]},{data:averageAt(heldout,'think.reward_mean'),color:colors[1]}]);draw('probeClosureChart',[{data:averageAt(original,'think.history_copy_rate'),color:colors[3]},{data:averageAt(heldout,'think.history_copy_rate'),color:colors[4]}])}
    const pageNav=`<div class="v5-probe-nav"><button data-v5-probe-page="standard" class="${probePage==='standard'?'active':''}">常规 Probe16</button><button data-v5-probe-page="history" class="${probePage==='history'?'active':''}">历史答案 Probe4</button></div>`;
    if(!selected){$('probeOverview').innerHTML='';$('probeDetail').innerHTML=`${pageNav}<div class="probe-empty">等待 ${probePage==='history'?'History-Copy Probe4':'Probe16'}</div>`;return}
    const d=selected.think||{},perDomain=probePage==='history'?1:4,coverage=probeCoverage(rows,selected.step,perDomain),domainCoverage=domainOrder.map(domain=>`${domain.toUpperCase()} ${coverage.counts[domain]}/${perDomain}`).join(' · ');setText('probeMeta',`${suite(selected)} · ${coverage.complete?`${coverage.expected}/${coverage.expected} complete`:`${coverage.total}/${coverage.expected} incomplete`} · fixed seed ${selected.seed} · Official Beam32 · ${sec(selected.probe_wall_sec)}`);
    diagnosticCards('probeOverview',[["Checkpoint coverage",`${coverage.total}/${coverage.expected} · ${coverage.complete?'COMPLETE':'INCOMPLETE'}`],["Domain coverage",domainCoverage],["Group / domain slot",`${selected.group_id} / ${selected.target_domain.toUpperCase()} ${probeSlot(rows,selected)}`],["Gold ∩ History Exact",`${d.gold_history_exact_overlap??0} / history ${d.history_sid_count??0}`],["Reward μ / σ",`${fmt(d.reward_mean)} / ${fmt(d.reward_std)}`],["Exact / AB / A",`${d.exact_count??0} / ${d.ab_count??0} / ${d.a_count??0}`],["History copy",`${d.history_copy_count??0}/${d.beam_candidate_count??128} · ${pct(d.history_copy_rate)}`],["Copy A / AB / Exact",`${d.copy_A??0} / ${d.copy_AB??0} / ${d.copy_Exact??0}`],["Non-copy A / AB / Exact",`${d.noncopy_A??0} / ${d.noncopy_AB??0} / ${d.noncopy_Exact??0}`],["CoT length / closure",`${fmt(d.cot_length_mean,1)} / ${pct(d.closure_rate)}`]]);
    const golds=(selected.gold_sids||[]).map(value=>`<span class="gold-chip">${escapeHtml(typeof value==='string'?value:sidV5(value))}</span>`).join(''),probeKey=`probe:${selected.probe_suite||'probe4'}:${selected.group_id}:${selected.step}`;
    const contract=probePage==='history'?`<div class="v5-contract v5-history-callout"><b>History-Copy Exact Probe4：</b>四域各 1 个固定 group，Gold 中分别有完整 SID 已出现在同域用户历史。该页专门观察模型是否复制历史 Exact、复制后 production reward、CoT 长度及 Beam32 copy anatomy；允许与训练集重合，不作为 unseen benchmark。</div>`:`<div class="v5-contract"><b>Official Probe16：</b>四域各 4 个固定 group；完整 checkpoint 共 16 groups × 4 CoT × Beam32 = 64 CoT / 2048 beams。四域曲线分别对域内 4 个 group 取均值。</div>`;
    $('probeDetail').innerHTML=`<div class="v5-dashboard">${pageNav}${domainLegend()}<div class="v5-probe-suite">${contract}<div class="v5-contract"><b>${coverage.complete?'覆盖完整':'覆盖未补齐'}：</b>${domainCoverage}。Beam32 每个完整 SID 都标出是否与同域历史 SID 完全一致；Probe 使用 production 原始 reward，不应用训练 copy discount。</div></div><section class="v5-group"><header class="v5-group-head"><div><div class="adv-title">${domainBadge(selected.target_domain)} ${suite(selected)} · Step ${selected.step} · 域内 ${probeSlot(rows,selected)}</div><div class="adv-sub">Gold∩History Exact: ${escapeHtml((d.gold_history_exact_sids||[]).map(sidV5).join(', ')||'-')} · Reward denominator: ${escapeHtml(d.reward_denominator||'per-CoT Beam32 hierarchical score')} · candidates ${d.candidate_count??4} CoT / ${d.beam_candidate_count??128} beams</div></div></header><details class="v5-source" ${detailAttr(`${probeKey}:source`)}><summary><span>Probe 模型输入原文</span><span>展开</span></summary><div class="v5-body"><pre class="v5-raw">${escapeHtml(selected.think_prompt||'')}</pre><div class="gold-strip"><span class="label">Gold SID</span>${golds}</div></div></details>${probeCandidates(selected,probeKey)}</section></div>`;
  }
  renderProbes=function(){if(isV5())renderV5Probes();else previousRenderProbes()};
})();
